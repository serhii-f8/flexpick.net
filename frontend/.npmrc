# Expose Astro dependencies for `pnpm` users
shamefully-hoist=true
# @astrolib/analytics and @astrolib/seo are unmaintained and declare
# peer deps only up to Astro 5, but work fine with Astro 6
legacy-peer-deps=true