dist
node_modules
.github
.changeset
design_handoff_landing_redesign

# Generated by `php artisan app:export-pricing` in backend/. Must stay
# byte-identical to that command's output, or CI's --check drift gate
# false-positives on Prettier's own reformatting.
src/data/pricing.json